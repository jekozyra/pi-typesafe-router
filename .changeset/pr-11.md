---
"pi-typesafe-router": minor
---

Release preparation now supports bootstrapping the initial 0.1.0 release when its npm package, Git tag, and GitHub Release are all absent, while continuing to reject partial or ambiguous release states.
