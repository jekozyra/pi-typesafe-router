---
"pi-typesafe-router": minor
---

Successful doctor verification now persists across reloads within the current session and is restored only when configuration, models, and credential references still match. Verification is invalidated after failures, changes, or navigation to a branch without valid proof.
