---
"pi-typesafe-router": patch
---

Release metadata files can now be up to 1 MiB without triggering the generated changeset size limit, while oversized files are rejected with clearer errors and their response bodies are cancelled.
